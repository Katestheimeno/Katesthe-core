# Docker Compose environment – copy to .env.local and use with docker compose
#   cp env.docker.example .env.local
# All services read from this file; docker-compose uses env_file only.
#
# Important: Inside the Docker network, pgbouncer listens on 5432.
# Use POSTGRES_PORT=5432 and DATABASE_URL with :5432 (not 6432).
# PGBOUNCER_PORT=6432 is only for host→container (e.g. connect from host to pgbouncer).

# Django
DJANGO_DEBUG=True
SECRET_KEY=!&c6j^rafut=16n%d^82%p)mke@n#1d^v3@*kxm@w%=si9@*iw
JWT_SECRET_KEY=v604wu=y^uxxyi0!jjembb%v(y#ff$$tq%sejf^ur$$!lh*yy=x9
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database – app talks to pgbouncer on 5432 inside the network
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=pgbouncer
POSTGRES_PORT=5432
POSTGRES_DB=drf_starter
DATABASE_URL=postgresql://postgres:postgres@pgbouncer:5432/drf_starter

# Pgbouncer: backend connection (edoburu/pgbouncer uses MD5 userlist; db runs with password_encryption=md5)
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=drf_starter
# Host port for pgbouncer (optional; default 6432)
PGBOUNCER_PORT=6432

# Redis
REDIS_URL=redis://redis:6379/0
CHANNELS_REDIS_URL=redis://redis:6379/0

# Web
WEB_PORT=8000

# Project
PROJECT_NAME=Katesthe-core
PROJECT_DESCRIPTION=A Django REST Framework starter project with ready-to-use authentication, custom user management, and modular app structure.
PROJECT_VERSION=1.0.0

# Contact
CONTACT_NAME=Katesthe-core Dev Team
CONTACT_EMAIL=support@katesthe-core.com
CONTACT_URL=https://github.com/katesthe-core

# Email (local / MailHog)
EMAIL_HOST=localhost
EMAIL_PORT=1025
EMAIL_USE_TLS=False
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_FRONTEND_DOMAIN=http://localhost:8000

# Theme
THEME_PRIMARY_COLOR=#6a0dad
THEME_ACCENT_COLOR=#4b0082

# Environment
DJANGO_ENV=local
DJANGO_SETTINGS_MODULE=config.django.local
